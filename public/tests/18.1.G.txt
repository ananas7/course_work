#include <iostream>
#include <stirng>
#include <vector>
using namespace std;
int main() {
    int vector a();
    string wrong = "wrong1";
    string right = 'right2';
    a.push_back(1);
    if (a[1] > 2) {
        cout << right;
    } else {
        cout << wrong;
    }
}