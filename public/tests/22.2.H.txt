#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector< vector<int> > O (5, vector<int> 5);
    int i = 0;
    for (;i < 5; ++i) {
        for (int j = 0; j < 5; ++j) {
            cin >> O[i][j];
        }
    }
}